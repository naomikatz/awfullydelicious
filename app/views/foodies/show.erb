<h1> Hey <%= @foodie.username %> </h1>

<%= button_to 'Logout', logout_path, method: :delete %>

<p><%= @foodie.username %></p>

<% if current_foodie.id == @foodie.id %>
  <%= button_to 'Delete this Account', foodie_path(@foodie), method: :delete, data: { confirm: 'are u sure lol' } %>
<% end %>
